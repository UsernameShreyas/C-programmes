#include<stdio.h>
int main()
{
    int marks[10],i,n.sum=0,average;
    printf("Enter the number of Elements:\t");
    scanf("%d\t",&n);

    for(i=0;i<10;++i)
    {
        printf("Enter Number %d\t", i+1);
        scanf("%d",&marks[i]);

        sum =marks[i];
    }

    average= sum/n;
    printf("")
}